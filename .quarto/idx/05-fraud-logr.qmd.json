{"title":"5 · Predicting fraud","markdown":{"yaml":{"title":"5 · Predicting fraud","subtitle":"From behavioural signals to flag probabilities — read with a fraud analyst's skepticism"},"headingText":"The analysis sample — and what the missing data quietly revealed","containsRefs":false,"markdown":"\n\nChapter 3 established that *which* token dominates an address's inflow is\nassociated with its fraud status. This chapter turns from association to\nconditional prediction: **can transaction behaviour — how much an address\ntransacts, how much of that is token activity, and how quickly it sends — predict\nthe likelihood of a fraud flag? And does the token ecosystem change what those\nbehaviours mean?** The goal is inferential understanding of the signals, not a\ndeployable classifier; that distinction ends up mattering more here than in any\nother chapter.\n\n\n```{r setup}\nlibrary(dplyr)\nlibrary(ggplot2)\nlibrary(broom)\nlibrary(car)\nlibrary(pROC)\n\ntrxs <- readRDS(\"data/eth_clean.rds\")\n\nlr <- trxs %>%\n  filter(!is.na(total_erc20_tnxs)) %>%\n  add_count(erc20_most_rec_token_type, name = \"type_n\") %>%\n  mutate(\n    token_rec  = ifelse(type_n < 40, \"Low Count\", erc20_most_rec_token_type),\n    token_rec  = relevel(factor(token_rec), ref = \"None\"),\n    tnx_log    = log10(total_transactions + 1),\n    erc20_log  = log10(total_erc20_tnxs + 1),\n    avgmin_log = log10(avg_min_between_sent_tnx + 1)\n  )\n\nn_excl  <- sum(is.na(trxs$total_erc20_tnxs))\nexcl_fr <- sum(trxs$flag[is.na(trxs$total_erc20_tnxs)])\ncat(nrow(lr), \"addresses in sample;\", sum(lr$flag), \"flagged\",\n    sprintf(\"(%.1f%%; majority-class baseline accuracy %.1f%%)\",\n            100 * mean(lr$flag), 100 * (1 - mean(lr$flag))))\n```\n\nThe model needs the ERC20 aggregates, so the `r n_excl` addresses lacking them are\nexcluded — and that routine step surfaced something worth pausing on. The fraud\nshare drops from 22.1% to `r sprintf(\"%.1f%%\", 100 * mean(lr$flag))`, which is a\nlarge move for a 8% exclusion, and the reason is stark: **all `r n_excl` of the\nexcluded addresses are flagged fraudulent** (`r excl_fr`/`r n_excl`). In other\nwords, in this dataset \"ERC20 data missing\" perfectly predicts the label. The most\nplausible cause is mechanical — the fraud-address list appears to have been\nassembled separately and joined in, with the ERC20 aggregates never computed for\npart of that batch — which makes missingness an artefact of dataset construction,\nnot a behaviour of fraudsters. The practical consequences: missingness must never\nbe used as a feature on this data, and everything below speaks for token-active\naddresses only. Low-frequency token types (< 40 addresses) are pooled into a\n`\"Low Count\"` level so group estimates stay stable, and `\"None\"` — no dominant\nERC20 inflow — is kept as the meaningful reference category.\n\n## Model 1: what behaviour alone says\n\n```{r m1}\nm1 <- glm(flag ~ tnx_log + erc20_log + avgmin_log,\n          family = binomial, data = lr)\n\ntidy(m1, exponentiate = TRUE, conf.int = TRUE) %>%\n  knitr::kable(digits = 3,\n               caption = \"Model 1 — odds ratios with 95% CIs. Each OR is the multiplicative change in fraud odds for a ×10 change in the predictor.\")\n```\n\n```{r m1-inline, include=FALSE}\nor1 <- exp(coef(m1))\nnull_model <- glm(flag ~ 1, family = binomial, data = lr)\nmcfadden <- function(m) 1 - as.numeric(logLik(m)) / as.numeric(logLik(null_model))\n```\n\nRead on the ×10 scale, the three effects sketch a coherent behavioural\nfingerprint. A ×10 increase in **ordinary transactions** multiplies the fraud odds\nby `r round(or1[[\"tnx_log\"]], 2)` — sustained, plain on-chain activity is what\nlegitimacy looks like. A ×10 increase in **ERC20 token transactions** multiplies\nthe odds by `r round(or1[[\"erc20_log\"]], 1)` — the flagged population is\ntoken-transfer-heavy. And a ×10 *longer* **average gap between sent transactions**\nmultiplies the odds by `r round(or1[[\"avgmin_log\"]], 2)`: quicker sending rhythms\nlean fraudulent, echoing the cadence difference found in Chapter 3. Fraud, in this\ndata, looks token-heavy, plain-transaction-light, and fast. The model's McFadden\npseudo-R² is `r round(mcfadden(m1), 2)` and the VIFs below confirm the three\nsignals are not proxies for one another.\n\n```{r m1-vif}\nvif(m1) %>% knitr::kable(col.names = \"VIF\",\n                         caption = \"Variance inflation factors — the three behavioural signals carry distinct information.\")\n```\n\n## From scores to decisions\n\nA logistic model outputs probabilities; a fraud screen must output decisions, and\nthe gap between those two is where the operational honesty lives.\n\n```{r m1-roc}\n#| fig-cap: \"Model 1 discrimination. AUC summarises ranking quality across all possible thresholds — it says nothing about any particular operating point.\"\nroc1 <- roc(lr$flag, fitted(m1), quiet = TRUE)\nplot(roc1, legacy.axes = TRUE, col = \"#2C7FB8\",\n     main = sprintf(\"Behaviour-only model — AUC %.2f\", auc(roc1)))\n```\n\n```{r m1-cm}\npred1 <- factor(ifelse(fitted(m1) >= 0.5, \"Fraudulent\", \"Legitimate\"),\n                levels = c(\"Legitimate\", \"Fraudulent\"))\ncm <- table(pred1, lr$flag_f)\n\ndata.frame(\n  ` ` = c(\"Predicted: Legitimate\", \"Predicted: Fraudulent\"),\n  `Actual: Legitimate` = cm[, \"Legitimate\"],\n  `Actual: Fraudulent` = cm[, \"Fraudulent\"],\n  check.names = FALSE\n) %>%\n  knitr::kable(row.names = FALSE,\n               caption = \"Confusion matrix at the default 0.5 threshold.\")\n\ntibble(\n  metric = c(\"AUC\", \"Accuracy\", \"Sensitivity (fraud recall)\", \"Specificity\"),\n  value = c(as.numeric(auc(roc1)),\n            sum(diag(cm)) / sum(cm),\n            cm[\"Fraudulent\", \"Fraudulent\"] / sum(cm[, \"Fraudulent\"]),\n            cm[\"Legitimate\", \"Legitimate\"] / sum(cm[, \"Legitimate\"]))\n) %>% knitr::kable(digits = 3, caption = \"Operating characteristics at the 0.5 threshold.\")\n```\n\nThe matrix tells the story the accuracy number hides. At the default threshold the\nmodel catches `r cm[\"Fraudulent\", \"Fraudulent\"]` of the\n`r sum(cm[, \"Fraudulent\"])` flagged addresses\n(sensitivity `r sprintf(\"%.0f%%\", 100 * cm[\"Fraudulent\",\"Fraudulent\"] / sum(cm[,\"Fraudulent\"]))`)\nwhile raising only `r cm[\"Fraudulent\", \"Legitimate\"]` false alarms — and still\nposts `r sprintf(\"%.0f%%\", 100 * sum(diag(cm)) / sum(cm))` accuracy, because with\n85% of addresses legitimate, accuracy mostly rewards saying \"legitimate\". The 0.5\nthreshold is a statistical default, not a policy: in a real fraud operation the\nthreshold is set by the relative cost of a missed fraud versus an investigation,\nand lowering it here would trade a manageable rise in false alarms for a large\ngain in recall. The behavioural signals rank addresses usefully\n(AUC `r round(as.numeric(auc(roc1)), 2)`); what to *do* with the ranking is a\nbusiness decision.\n\n## Model 2: does the token ecosystem change what behaviour means?\n\nChapter 3 found the dominant received token *associated* with fraud; the sharper\nquestion is whether it *moderates* the behavioural signals — whether the same\npattern of activity means something different inside different token communities.\nModel 2 interacts all three behavioural predictors with the token factor.\n\n```{r m2}\nm2 <- glm(flag ~ (tnx_log + erc20_log + avgmin_log) * token_rec,\n          family = binomial, data = lr)\n\n# Wald intervals: several token × behaviour cells contain (almost) only one\n# class, so profile-likelihood CIs do not converge for those terms.\ntidy(m2) %>%\n  mutate(OR = exp(estimate),\n         conf.low = exp(estimate - 1.96 * std.error),\n         conf.high = exp(estimate + 1.96 * std.error)) %>%\n  select(term, OR, conf.low, conf.high, p.value) %>%\n  filter(p.value < 0.05) %>%\n  arrange(p.value) %>%\n  knitr::kable(digits = 3,\n               caption = \"Model 2 — significant terms only (Wald 95% CIs; full table in the folded code).\")\n```\n\nThe table already hints at what is happening: token-level odds ratios in the\nhundreds of thousands, interaction ORs indistinguishable from zero. Estimates that\nextreme are the signature of near-empty cells — token groups where one class\nbarely occurs — pushing coefficients toward the boundary. They are shown for\ncompleteness, not interpreted individually; the trustworthy comparison is at the\nmodel level.\n\n```{r compare}\nlrt <- anova(m1, m2, test = \"Chisq\")\nroc2 <- roc(lr$flag, fitted(m2), quiet = TRUE)\n\ntibble(model = c(\"M1: behaviour only\", \"M2: + token-type interactions\"),\n       pseudo_R2 = c(mcfadden(m1), mcfadden(m2)),\n       AUC = c(as.numeric(auc(roc1)), as.numeric(auc(roc2))),\n       AIC = c(AIC(m1), AIC(m2))) %>%\n  knitr::kable(digits = 3,\n               caption = \"Model comparison. The likelihood-ratio test for the added terms is decisive (p < .001) — the interesting question is why.\")\n```\n\n```{r roc-both}\n#| fig-cap: \"Both models' ROC curves. The jump is real in-sample — and the next paragraphs explain why it should not be taken at face value.\"\nplot(roc1, col = \"#2C7FB8\", legacy.axes = TRUE,\n     main = \"Adding token context: dramatic — and suspicious\")\nplot(roc2, col = \"#D95F02\", add = TRUE)\nlegend(\"bottomright\",\n       legend = c(sprintf(\"M1: behaviour only (AUC %.2f)\", auc(roc1)),\n                  sprintf(\"M2: + token context (AUC %.2f)\", auc(roc2))),\n       col = c(\"#2C7FB8\", \"#D95F02\"), lwd = 2, bty = \"n\")\n```\n\n```{r leakage, include=FALSE}\nperfect <- lr %>%\n  group_by(token_rec) %>%\n  summarise(n = n(), fraud_rate = mean(flag), .groups = \"drop\") %>%\n  filter(fraud_rate == 1)\n```\n\n### Reading the jump carefully\n\nAn AUC of `r round(as.numeric(auc(roc2)), 2)` from a logistic regression should\nraise an eyebrow before it raises applause, and here the skepticism pays off. Part\nof the improvement is genuine segment structure — Chapter 3's association result\nmade that expected. But a decisive part of it is simpler:\n**`r nrow(perfect)` token levels in this sample coincide perfectly with the fraud\nflag** — `r paste(sprintf(\"*%s* (n = %d)\", perfect$token_rec, perfect$n), collapse = \" and \")`\nare flagged fraudulent without exception. Both are known spam-airdrop tokens, and\nthe likely mechanism runs through how the dataset was built: the fraud list appears\nto draw heavily on addresses swept up in those scam ecosystems, so receiving these\ntokens is partly a *consequence* of how the label was constructed rather than an\nindependent behavioural signal. Combined with the missingness artefact found\nearlier, the message is consistent: this dataset rewards models that memorise its\nconstruction.\n\nFor anyone taking this analysis toward a production model, three practices follow\nnaturally: validate out-of-time rather than in-sample, so construction artefacts\nstop paying; ablate token *identity* and check how much discrimination survives on\nbehaviour alone (here, an AUC of `r round(as.numeric(auc(roc1)), 2)`); and treat\ntoken context as segment information whose meaning must be re-learned as scam\necosystems change, not as a stable feature. Read that way, Model 2 is best\nunderstood as strong evidence that token context *carries* signal — and a worked\nexample of why a fraud analyst never deploys an in-sample 0.98.\n\n## What this chapter established\n\nBehaviour alone ranks addresses usefully — token-heavy, plain-transaction-light,\nfast-cadence profiles carry materially higher flag odds — and token context adds\nreal but contaminated lift whose honest assessment requires validation this\ncross-sectional dataset cannot provide. The estimates speak for token-active\naddresses, against a label of undocumented provenance, in-sample only; the\ndeployment-grade treatment of this problem — held-out protocols, imbalance\nhandling, cost-sensitive thresholds — is the subject of the dedicated\nmachine-learning project that this suite's conclusions point toward.\n","srcMarkdownNoYaml":"\n\nChapter 3 established that *which* token dominates an address's inflow is\nassociated with its fraud status. This chapter turns from association to\nconditional prediction: **can transaction behaviour — how much an address\ntransacts, how much of that is token activity, and how quickly it sends — predict\nthe likelihood of a fraud flag? And does the token ecosystem change what those\nbehaviours mean?** The goal is inferential understanding of the signals, not a\ndeployable classifier; that distinction ends up mattering more here than in any\nother chapter.\n\n## The analysis sample — and what the missing data quietly revealed\n\n```{r setup}\nlibrary(dplyr)\nlibrary(ggplot2)\nlibrary(broom)\nlibrary(car)\nlibrary(pROC)\n\ntrxs <- readRDS(\"data/eth_clean.rds\")\n\nlr <- trxs %>%\n  filter(!is.na(total_erc20_tnxs)) %>%\n  add_count(erc20_most_rec_token_type, name = \"type_n\") %>%\n  mutate(\n    token_rec  = ifelse(type_n < 40, \"Low Count\", erc20_most_rec_token_type),\n    token_rec  = relevel(factor(token_rec), ref = \"None\"),\n    tnx_log    = log10(total_transactions + 1),\n    erc20_log  = log10(total_erc20_tnxs + 1),\n    avgmin_log = log10(avg_min_between_sent_tnx + 1)\n  )\n\nn_excl  <- sum(is.na(trxs$total_erc20_tnxs))\nexcl_fr <- sum(trxs$flag[is.na(trxs$total_erc20_tnxs)])\ncat(nrow(lr), \"addresses in sample;\", sum(lr$flag), \"flagged\",\n    sprintf(\"(%.1f%%; majority-class baseline accuracy %.1f%%)\",\n            100 * mean(lr$flag), 100 * (1 - mean(lr$flag))))\n```\n\nThe model needs the ERC20 aggregates, so the `r n_excl` addresses lacking them are\nexcluded — and that routine step surfaced something worth pausing on. The fraud\nshare drops from 22.1% to `r sprintf(\"%.1f%%\", 100 * mean(lr$flag))`, which is a\nlarge move for a 8% exclusion, and the reason is stark: **all `r n_excl` of the\nexcluded addresses are flagged fraudulent** (`r excl_fr`/`r n_excl`). In other\nwords, in this dataset \"ERC20 data missing\" perfectly predicts the label. The most\nplausible cause is mechanical — the fraud-address list appears to have been\nassembled separately and joined in, with the ERC20 aggregates never computed for\npart of that batch — which makes missingness an artefact of dataset construction,\nnot a behaviour of fraudsters. The practical consequences: missingness must never\nbe used as a feature on this data, and everything below speaks for token-active\naddresses only. Low-frequency token types (< 40 addresses) are pooled into a\n`\"Low Count\"` level so group estimates stay stable, and `\"None\"` — no dominant\nERC20 inflow — is kept as the meaningful reference category.\n\n## Model 1: what behaviour alone says\n\n```{r m1}\nm1 <- glm(flag ~ tnx_log + erc20_log + avgmin_log,\n          family = binomial, data = lr)\n\ntidy(m1, exponentiate = TRUE, conf.int = TRUE) %>%\n  knitr::kable(digits = 3,\n               caption = \"Model 1 — odds ratios with 95% CIs. Each OR is the multiplicative change in fraud odds for a ×10 change in the predictor.\")\n```\n\n```{r m1-inline, include=FALSE}\nor1 <- exp(coef(m1))\nnull_model <- glm(flag ~ 1, family = binomial, data = lr)\nmcfadden <- function(m) 1 - as.numeric(logLik(m)) / as.numeric(logLik(null_model))\n```\n\nRead on the ×10 scale, the three effects sketch a coherent behavioural\nfingerprint. A ×10 increase in **ordinary transactions** multiplies the fraud odds\nby `r round(or1[[\"tnx_log\"]], 2)` — sustained, plain on-chain activity is what\nlegitimacy looks like. A ×10 increase in **ERC20 token transactions** multiplies\nthe odds by `r round(or1[[\"erc20_log\"]], 1)` — the flagged population is\ntoken-transfer-heavy. And a ×10 *longer* **average gap between sent transactions**\nmultiplies the odds by `r round(or1[[\"avgmin_log\"]], 2)`: quicker sending rhythms\nlean fraudulent, echoing the cadence difference found in Chapter 3. Fraud, in this\ndata, looks token-heavy, plain-transaction-light, and fast. The model's McFadden\npseudo-R² is `r round(mcfadden(m1), 2)` and the VIFs below confirm the three\nsignals are not proxies for one another.\n\n```{r m1-vif}\nvif(m1) %>% knitr::kable(col.names = \"VIF\",\n                         caption = \"Variance inflation factors — the three behavioural signals carry distinct information.\")\n```\n\n## From scores to decisions\n\nA logistic model outputs probabilities; a fraud screen must output decisions, and\nthe gap between those two is where the operational honesty lives.\n\n```{r m1-roc}\n#| fig-cap: \"Model 1 discrimination. AUC summarises ranking quality across all possible thresholds — it says nothing about any particular operating point.\"\nroc1 <- roc(lr$flag, fitted(m1), quiet = TRUE)\nplot(roc1, legacy.axes = TRUE, col = \"#2C7FB8\",\n     main = sprintf(\"Behaviour-only model — AUC %.2f\", auc(roc1)))\n```\n\n```{r m1-cm}\npred1 <- factor(ifelse(fitted(m1) >= 0.5, \"Fraudulent\", \"Legitimate\"),\n                levels = c(\"Legitimate\", \"Fraudulent\"))\ncm <- table(pred1, lr$flag_f)\n\ndata.frame(\n  ` ` = c(\"Predicted: Legitimate\", \"Predicted: Fraudulent\"),\n  `Actual: Legitimate` = cm[, \"Legitimate\"],\n  `Actual: Fraudulent` = cm[, \"Fraudulent\"],\n  check.names = FALSE\n) %>%\n  knitr::kable(row.names = FALSE,\n               caption = \"Confusion matrix at the default 0.5 threshold.\")\n\ntibble(\n  metric = c(\"AUC\", \"Accuracy\", \"Sensitivity (fraud recall)\", \"Specificity\"),\n  value = c(as.numeric(auc(roc1)),\n            sum(diag(cm)) / sum(cm),\n            cm[\"Fraudulent\", \"Fraudulent\"] / sum(cm[, \"Fraudulent\"]),\n            cm[\"Legitimate\", \"Legitimate\"] / sum(cm[, \"Legitimate\"]))\n) %>% knitr::kable(digits = 3, caption = \"Operating characteristics at the 0.5 threshold.\")\n```\n\nThe matrix tells the story the accuracy number hides. At the default threshold the\nmodel catches `r cm[\"Fraudulent\", \"Fraudulent\"]` of the\n`r sum(cm[, \"Fraudulent\"])` flagged addresses\n(sensitivity `r sprintf(\"%.0f%%\", 100 * cm[\"Fraudulent\",\"Fraudulent\"] / sum(cm[,\"Fraudulent\"]))`)\nwhile raising only `r cm[\"Fraudulent\", \"Legitimate\"]` false alarms — and still\nposts `r sprintf(\"%.0f%%\", 100 * sum(diag(cm)) / sum(cm))` accuracy, because with\n85% of addresses legitimate, accuracy mostly rewards saying \"legitimate\". The 0.5\nthreshold is a statistical default, not a policy: in a real fraud operation the\nthreshold is set by the relative cost of a missed fraud versus an investigation,\nand lowering it here would trade a manageable rise in false alarms for a large\ngain in recall. The behavioural signals rank addresses usefully\n(AUC `r round(as.numeric(auc(roc1)), 2)`); what to *do* with the ranking is a\nbusiness decision.\n\n## Model 2: does the token ecosystem change what behaviour means?\n\nChapter 3 found the dominant received token *associated* with fraud; the sharper\nquestion is whether it *moderates* the behavioural signals — whether the same\npattern of activity means something different inside different token communities.\nModel 2 interacts all three behavioural predictors with the token factor.\n\n```{r m2}\nm2 <- glm(flag ~ (tnx_log + erc20_log + avgmin_log) * token_rec,\n          family = binomial, data = lr)\n\n# Wald intervals: several token × behaviour cells contain (almost) only one\n# class, so profile-likelihood CIs do not converge for those terms.\ntidy(m2) %>%\n  mutate(OR = exp(estimate),\n         conf.low = exp(estimate - 1.96 * std.error),\n         conf.high = exp(estimate + 1.96 * std.error)) %>%\n  select(term, OR, conf.low, conf.high, p.value) %>%\n  filter(p.value < 0.05) %>%\n  arrange(p.value) %>%\n  knitr::kable(digits = 3,\n               caption = \"Model 2 — significant terms only (Wald 95% CIs; full table in the folded code).\")\n```\n\nThe table already hints at what is happening: token-level odds ratios in the\nhundreds of thousands, interaction ORs indistinguishable from zero. Estimates that\nextreme are the signature of near-empty cells — token groups where one class\nbarely occurs — pushing coefficients toward the boundary. They are shown for\ncompleteness, not interpreted individually; the trustworthy comparison is at the\nmodel level.\n\n```{r compare}\nlrt <- anova(m1, m2, test = \"Chisq\")\nroc2 <- roc(lr$flag, fitted(m2), quiet = TRUE)\n\ntibble(model = c(\"M1: behaviour only\", \"M2: + token-type interactions\"),\n       pseudo_R2 = c(mcfadden(m1), mcfadden(m2)),\n       AUC = c(as.numeric(auc(roc1)), as.numeric(auc(roc2))),\n       AIC = c(AIC(m1), AIC(m2))) %>%\n  knitr::kable(digits = 3,\n               caption = \"Model comparison. The likelihood-ratio test for the added terms is decisive (p < .001) — the interesting question is why.\")\n```\n\n```{r roc-both}\n#| fig-cap: \"Both models' ROC curves. The jump is real in-sample — and the next paragraphs explain why it should not be taken at face value.\"\nplot(roc1, col = \"#2C7FB8\", legacy.axes = TRUE,\n     main = \"Adding token context: dramatic — and suspicious\")\nplot(roc2, col = \"#D95F02\", add = TRUE)\nlegend(\"bottomright\",\n       legend = c(sprintf(\"M1: behaviour only (AUC %.2f)\", auc(roc1)),\n                  sprintf(\"M2: + token context (AUC %.2f)\", auc(roc2))),\n       col = c(\"#2C7FB8\", \"#D95F02\"), lwd = 2, bty = \"n\")\n```\n\n```{r leakage, include=FALSE}\nperfect <- lr %>%\n  group_by(token_rec) %>%\n  summarise(n = n(), fraud_rate = mean(flag), .groups = \"drop\") %>%\n  filter(fraud_rate == 1)\n```\n\n### Reading the jump carefully\n\nAn AUC of `r round(as.numeric(auc(roc2)), 2)` from a logistic regression should\nraise an eyebrow before it raises applause, and here the skepticism pays off. Part\nof the improvement is genuine segment structure — Chapter 3's association result\nmade that expected. But a decisive part of it is simpler:\n**`r nrow(perfect)` token levels in this sample coincide perfectly with the fraud\nflag** — `r paste(sprintf(\"*%s* (n = %d)\", perfect$token_rec, perfect$n), collapse = \" and \")`\nare flagged fraudulent without exception. Both are known spam-airdrop tokens, and\nthe likely mechanism runs through how the dataset was built: the fraud list appears\nto draw heavily on addresses swept up in those scam ecosystems, so receiving these\ntokens is partly a *consequence* of how the label was constructed rather than an\nindependent behavioural signal. Combined with the missingness artefact found\nearlier, the message is consistent: this dataset rewards models that memorise its\nconstruction.\n\nFor anyone taking this analysis toward a production model, three practices follow\nnaturally: validate out-of-time rather than in-sample, so construction artefacts\nstop paying; ablate token *identity* and check how much discrimination survives on\nbehaviour alone (here, an AUC of `r round(as.numeric(auc(roc1)), 2)`); and treat\ntoken context as segment information whose meaning must be re-learned as scam\necosystems change, not as a stable feature. Read that way, Model 2 is best\nunderstood as strong evidence that token context *carries* signal — and a worked\nexample of why a fraud analyst never deploys an in-sample 0.98.\n\n## What this chapter established\n\nBehaviour alone ranks addresses usefully — token-heavy, plain-transaction-light,\nfast-cadence profiles carry materially higher flag odds — and token context adds\nreal but contaminated lift whose honest assessment requires validation this\ncross-sectional dataset cannot provide. The estimates speak for token-active\naddresses, against a label of undocumented provenance, in-sample only; the\ndeployment-grade treatment of this problem — held-out protocols, imbalance\nhandling, cost-sensitive thresholds — is the subject of the dedicated\nmachine-learning project that this suite's conclusions point toward.\n"},"formats":{"html":{"identifier":{"display-name":"HTML","target-format":"html","base-format":"html"},"execute":{"fig-width":8,"fig-height":5,"fig-format":"retina","fig-dpi":96,"df-print":"default","error":false,"eval":true,"cache":null,"freeze":false,"echo":true,"output":true,"warning":false,"include":true,"keep-md":false,"keep-ipynb":false,"ipynb":null,"enabled":null,"daemon":null,"daemon-restart":false,"debug":false,"ipynb-filters":[],"ipynb-shell-interactivity":null,"plotly-connected":true,"message":false,"engine":"knitr"},"render":{"keep-tex":false,"keep-typ":false,"keep-source":false,"keep-hidden":false,"prefer-html":false,"output-divs":true,"output-ext":"html","fig-align":"default","fig-pos":null,"fig-env":null,"code-fold":true,"code-overflow":"scroll","code-link":false,"code-line-numbers":false,"code-tools":true,"tbl-colwidths":"auto","merge-includes":true,"inline-includes":false,"preserve-yaml":false,"latex-auto-mk":true,"latex-auto-install":true,"latex-clean":true,"latex-min-runs":1,"latex-max-runs":10,"latex-makeindex":"makeindex","latex-makeindex-opts":[],"latex-tlmgr-opts":[],"latex-input-paths":[],"latex-output-dir":null,"link-external-icon":false,"link-external-newwindow":false,"self-contained-math":false,"format-resources":[],"notebook-links":true},"pandoc":{"standalone":true,"wrap":"none","default-image-extension":"png","to":"html","css":["styles.css"],"toc":true,"output-file":"05-fraud-logr.html"},"language":{"toc-title-document":"Table of contents","toc-title-website":"On this page","related-formats-title":"Other Formats","related-notebooks-title":"Notebooks","source-notebooks-prefix":"Source","other-links-title":"Other Links","code-links-title":"Code Links","launch-dev-container-title":"Launch Dev Container","launch-binder-title":"Launch Binder","article-notebook-label":"Article Notebook","notebook-preview-download":"Download Notebook","notebook-preview-download-src":"Download Source","notebook-preview-back":"Back to Article","manuscript-meca-bundle":"MECA Bundle","section-title-abstract":"Abstract","section-title-appendices":"Appendices","section-title-footnotes":"Footnotes","section-title-references":"References","section-title-reuse":"Reuse","section-title-copyright":"Copyright","section-title-citation":"Citation","appendix-attribution-cite-as":"For attribution, please cite this work as:","appendix-attribution-bibtex":"BibTeX citation:","appendix-view-license":"View License","title-block-author-single":"Author","title-block-author-plural":"Authors","title-block-affiliation-single":"Affiliation","title-block-affiliation-plural":"Affiliations","title-block-published":"Published","title-block-modified":"Modified","title-block-keywords":"Keywords","callout-tip-title":"Tip","callout-note-title":"Note","callout-warning-title":"Warning","callout-important-title":"Important","callout-caution-title":"Caution","code-summary":"Code","code-tools-menu-caption":"Code","code-tools-show-all-code":"Show All Code","code-tools-hide-all-code":"Hide All Code","code-tools-view-source":"View Source","code-tools-source-code":"Source Code","tools-share":"Share","tools-download":"Download","code-line":"Line","code-lines":"Lines","copy-button-tooltip":"Copy to Clipboard","copy-button-tooltip-success":"Copied!","repo-action-links-edit":"Edit this page","repo-action-links-source":"View source","repo-action-links-issue":"Report an issue","back-to-top":"Back to top","search-no-results-text":"No results","search-matching-documents-text":"matching documents","search-copy-link-title":"Copy link to search","search-hide-matches-text":"Hide additional matches","search-more-match-text":"more match in this document","search-more-matches-text":"more matches in this document","search-clear-button-title":"Clear","search-text-placeholder":"","search-detached-cancel-button-title":"Cancel","search-submit-button-title":"Submit","search-label":"Search","toggle-section":"Toggle section","toggle-sidebar":"Toggle sidebar navigation","toggle-dark-mode":"Toggle dark mode","toggle-reader-mode":"Toggle reader mode","toggle-navigation":"Toggle navigation","crossref-fig-title":"Figure","crossref-tbl-title":"Table","crossref-lst-title":"Listing","crossref-thm-title":"Theorem","crossref-lem-title":"Lemma","crossref-cor-title":"Corollary","crossref-prp-title":"Proposition","crossref-cnj-title":"Conjecture","crossref-def-title":"Definition","crossref-exm-title":"Example","crossref-exr-title":"Exercise","crossref-ch-prefix":"Chapter","crossref-apx-prefix":"Appendix","crossref-sec-prefix":"Section","crossref-eq-prefix":"Equation","crossref-lof-title":"List of Figures","crossref-lot-title":"List of Tables","crossref-lol-title":"List of Listings","environment-proof-title":"Proof","environment-remark-title":"Remark","environment-solution-title":"Solution","listing-page-order-by":"Order By","listing-page-order-by-default":"Default","listing-page-order-by-date-asc":"Oldest","listing-page-order-by-date-desc":"Newest","listing-page-order-by-number-desc":"High to Low","listing-page-order-by-number-asc":"Low to High","listing-page-field-date":"Date","listing-page-field-title":"Title","listing-page-field-description":"Description","listing-page-field-author":"Author","listing-page-field-filename":"File Name","listing-page-field-filemodified":"Modified","listing-page-field-subtitle":"Subtitle","listing-page-field-readingtime":"Reading Time","listing-page-field-wordcount":"Word Count","listing-page-field-categories":"Categories","listing-page-minutes-compact":"{0} min","listing-page-category-all":"All","listing-page-no-matches":"No matching items","listing-page-words":"{0} words","listing-page-filter":"Filter","draft":"Draft"},"metadata":{"lang":"en","fig-responsive":true,"quarto-version":"1.9.38","engines":[{"path":"C:\\Users\\tasko\\AppData\\Local\\Programs\\Quarto\\share\\extension-subtrees\\julia-engine\\_extensions\\julia-engine\\julia-engine.js"}],"theme":"cosmo","title":"5 · Predicting fraud","subtitle":"From behavioural signals to flag probabilities — read with a fraud analyst's skepticism"},"extensions":{"book":{"multiFile":true}}}},"projectFormats":["html"]}